# Lesson Notification System (Django + Celery + Docker)

Тестовое задание, реализующее асинхронную систему уведомлений.
При создании нового урока (`Lesson`) срабатывает Django Signal, который отправляет задачу в Celery. Воркер обрабатывает задачу в фоновом режиме.

## 🛠 Стек технологий


* **Python 3.12**
* **Django 4** — веб-фреймворк.
* **Celery** — очередь задач.
* **Redis** — брокер сообщений.
* **PostgreSQL** — база данных.
* **Flower** — мониторинг задач Celery.
* **Docker & Docker Compose** — контейнеризация и оркестрация.
* **uv** — менеджер пакетов (используется внутри Docker).

---

## 🚀 Быстрый запуск

Для запуска проекта вам потребуется только установленный **Docker**.

### 1. Клонирование и настройка окружения

Скачайте репозиторий и создайте файл переменных окружения:

```bash
git clone https://github.com/Kinin812/lesson_project.git
cd lesson_project

# Переименовываем файл .env.example в .env (в рамках задания не делаем акцент на секреты) ))
cp .env.example .env
```

### 2. Запуск контейнеров

Выполните команду для сборки и запуска всех сервисов (Django, DB, Redis, Celery, Flower):
```bash
docker-compose up --build -d
```

### 3. Создание суперпользователя
Чтобы попасть в админку, создайте администратора:
```bash
docker-compose exec web python manage.py createsuperuser
```

### 4. Как запустить тесты

Нужно запустить команду test внутри контейнера web.

```bash
docker-compose exec web python manage.py test lessons
```


## 🧪 Как проверить работу

Откройте админ-панель: Перейдите по адресу [http://localhost:8000/admin/](http://localhost:8000/admin/) и войдите под созданным суперпользователем.

Откройте мониторинг Flower (в новой вкладке): Перейдите по адресу [http://localhost:5555](http://localhost:5555).

Создайте урок: В админке зайдите в раздел Lessons -> Add Lesson.

 - Заполните Title и выберите Student.

 - Нажмите Save.

    #### ** альтернатива: Можно использовать Django shell для создания урока:

```bash
docker-compose exec web python manage.py shell
```
```python
from lessons.models import Lesson
lesson = Lesson.objects.create(title="New lesson", student_id=123)
```

Проверьте результат:

 - В Flower: Вы увидите новую задачу lessons.tasks.send_lesson_notification со статусом SUCCESS.

 - В логах: В терминале (docker-compose logs -f celery) появится сообщение об отправке уведомления.



### Authors
[Kinin812](https://github.com/Kinin812)